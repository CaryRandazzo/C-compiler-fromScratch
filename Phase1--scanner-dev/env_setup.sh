# Install the flex scanner generator
sudo apt-get install flex -y

# Install a c compiler
# sudo apt install gccG

# Alternatively, install other tools such as
# - GNU C compiler
# - libc6-dev  (header files for GNU C Library, glibc)
# - g++  (the GNU c++ compiler)
# - make (build tool for medium/large software packages)
# - dpkg-dev (build a package in .deb packaging format used by debian and ubuntu)
# using ...
sudo apt install build-essential